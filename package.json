{
  "name": "partnermarket-global-crm",
  "version": "2.5.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare-native personal relationship CRM with integrated business email and prospect campaigns",
  "scripts": {
    "dev": "wrangler dev --var AUTH_MODE:dev --var ENVIRONMENT:development",
    "dev:mock": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/worker.js && node --check src/email.js && node --check src/email-worker.js && node --check src/intelligence.js && node --check src/prospecting.js && node --check src/lib/domain.js && node --check src/lib/email.js && node --check public/app.js && node --check public/email.js && node --check public/intelligence.js && node --check public/prospecting.js && node --check src/reporting.js && node --check public/reporting.js && node --check scripts/dev-server.mjs && node --check scripts/preflight-production.mjs",
    "validate": "npm run check && npm test",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply partnermarket-crm-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply partnermarket-crm-db --remote",
    "cf:types": "wrangler types",
    "deploy:email": "wrangler deploy --config wrangler.email.jsonc",
    "deploy:all": "npm run deploy:email && npm run deploy",
    "preflight:production": "node scripts/preflight-production.mjs"
  },
  "devDependencies": {
    "wrangler": "4.113.0"
  },
  "overrides": {
    "sharp": "0.35.3"
  },
  "engines": {
    "node": ">=20"
  }
}
